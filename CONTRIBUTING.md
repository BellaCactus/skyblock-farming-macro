# contributing

if you wanna tweak stuff:
- keep it AutoHotkey **v2** compatible
- prefer small focused changes
- explain what the change fixes (and why)

## local testing
just run `farming-macro.ahk` by double-clicking (autoHotkey v2 installed).
